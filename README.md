# Vendor Management System

A full-stack Ramp (Vendors Page) developed as part of a take-home assessment.
The project is built with FastAPI on the backend and React + TypeScript on the frontend.

## Project Structure

- `/frontend` - React + TypeScript + Vite frontend
- `/backend` - FastAPI Python backend
